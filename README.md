# maven-log-parser
Tool which parses maven test log and prints short summary of test results.

## Installation

Run following command

```
# ./install.sh
```

## Usage

You can use the tool by command maven-log-parser. For detail info about usage, run

```
$ maven-log-parser -help
```
